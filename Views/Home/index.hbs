<div class="hero-section bg-light py-5 mb-5">
    <div class="container">
        <div class="row align-items-center">
            <div class="col-md-6">
                <h1>Добро пожаловать в BRAND SHOP</h1>
                <p class="lead">Только оригинальные брендовые товары по лучшим ценам</p>
                <a href="/products" class="btn btn-primary btn-lg">Начать покупки</a>
            </div>
            <div class="col-md-6 text-center">
                <img src="https://via.placeholder.com/500x300/FF6B6B/FFFFFF?text=BRAND+SHOP" alt="BRAND SHOP" class="img-fluid rounded">
            </div>
        </div>
    </div>
</div>

<div class="container">
    <div class="row">
        <div class="col-12">
            <h2 class="mb-4">Популярные товары</h2>
        </div>
    </div>
    
    <div class="row">
        {{#if popularProducts}}
            {{#each popularProducts}}
            <div class="col-md-3 col-sm-6 mb-4">
                <div class="card product-card h-100">
                    <img src="{{#if image_url}}{{image_url}}{{else}}https://via.placeholder.com/200x200/CCCCCC/FFFFFF?text=No+Image{{/if}}" class="card-img-top" alt="{{name}}">
                    <div class="card-body d-flex flex-column">
                        <h5 class="card-title">{{name}}</h5>
                        <p class="card-text flex-grow-1">{{brand_name}}</p>
                        <div class="mt-auto">
                            <div class="d-flex justify-content-between align-items-center">
                                <strong class="text-primary">{{price}} ₽</strong>
                                <a href="/products/{{id}}" class="btn btn-sm btn-outline-primary">Подробнее</a>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
            {{/each}}
        {{else}}
            <div class="col-12">
                <p class="text-center">Нет доступных товаров</p>
            </div>
        {{/if}}
    </div>
</div>